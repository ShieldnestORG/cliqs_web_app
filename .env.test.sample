# Test environment – copy to .env.test.local for local test runs
# Jest uses jest.setup.js mocks; these are for integration/e2e if needed

NEXT_PUBLIC_MULTICHAIN=true
NEXT_PUBLIC_REGISTRY_NAME=cosmoshub
NEXT_PUBLIC_TESTNETS_ENABLED=false

# Leave empty for tests – DB calls are mocked
MONGODB_URI=
MONGODB_DB_NAME=cliqs
