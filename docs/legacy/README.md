# Legacy Schema

## db-schema.graphql

Legacy DGraph GraphQL schema from the original Cosmos Multisig UI. The project no longer uses DGraph; it uses MongoDB Atlas or local JSON storage. This file is kept for historical reference only and is not used at runtime.
